# No secrets needed for P0. All runtime configuration lives in wrangler.toml
# [vars] and all dashboard auth is handled by Cloudflare Access. This file
# exists only as a signal to future contributors.
